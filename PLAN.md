# SwiftLM Plan

## P1 — Add new model tests

Tests and bench entries are needed for models that are downloaded but not yet covered:

- [ ] Qwen3-Embedding-8B — add to bench.sh MODELS table (embed section) and write tests
- [ ] Qwen3-VL-4B-Instruct-4bit — add to bench.sh (VLM section) and write tests
- [ ] DeepSeek-V3.2-mlx-5bit — add to bench.sh (SSD section) and write tests
- [ ] Kimi-K2-Instruct-4bit — add to bench.sh (SSD section) and write tests
